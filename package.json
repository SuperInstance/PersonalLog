{
  "name": "personallog",
  "version": "1.0.0",
  "description": "SuperInstance personal logging app for everyday use",
  "private": true,
  "license": "MIT",
  "author": "SuperInstance",
  "homepage": "https://github.com/SuperInstance/PersonalLog#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/SuperInstance/PersonalLog.git"
  },
  "bugs": {
    "url": "https://github.com/SuperInstance/PersonalLog/issues"
  },
  "scripts": {
    "dev": "npm run verify:wasm || true && next dev -p 3002",
    "build": "npm run verify:wasm || npm run build:wasm:missing-error && next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "test": "npm run test:wasm && npm run type-check",
    "test:unit": "vitest run",
    "test:watch": "vitest watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.config.integration.ts",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:headed": "playwright test --headed",
    "test:a11y": "playwright test tests/a11y/",
    "test:perf": "playwright test tests/performance/",
    "test:all": "npm run test:unit && npm run test:integration && npm run test:e2e",
    "clean": "npm run clean:wasm && rm -rf .next out coverage playwright-report",
    "clean:wasm": "rm -rf native/rust/pkg native/rust/target",
    "build:wasm": "cd native/rust && wasm-pack build --target web --weak-refs --out-dir pkg",
    "build:wasm:release": "cd native/rust && wasm-pack build --release --target web --weak-refs --out-dir pkg",
    "build:wasm:missing-error": "echo \"Error: wasm-pack not found. Install Rust toolchain: https://www.rust-lang.org/tools/install\" && exit 1",
    "watch:wasm": "cd native/rust && cargo watch -s 'wasm-pack build --target web --weak-refs --out-dir pkg'",
    "test:wasm": "cd native/rust && wasm-pack test --node",
    "test:wasm:browser": "cd native/rust && wasm-pack test --firefox",
    "verify:wasm": "npm run build:wasm && node scripts/verify-wasm.js",
    "verify:wasm:existing": "node scripts/verify-wasm.js",
    "predev": "npm run build:wasm || echo \"Warning: WASM build failed. Run 'npm run build:wasm' after installing Rust toolchain.\"",
    "prebuild": "npm run build:wasm:release || echo \"Warning: WASM release build failed. Run 'npm run build:wasm:release' after installing Rust toolchain.\""
  },
  "dependencies": {
    "next": "15.3.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "lucide-react": "^0.525.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@testing-library/react": "^14.0.0",
    "@testing-library/jest-dom": "^6.1.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4.2.0",
    "@vitest/ui": "^1.0.0",
    "axe-core": "^4.8.0",
    "axe-playwright": "^1.2.0",
    "eslint": "^9",
    "eslint-config-next": "16.1.1",
    "jsdom": "^23.0.0",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "keywords": [
    "superinstance",
    "personal",
    "logging",
    "journal",
    "notes",
    "productivity",
    "nextjs"
  ]
}
